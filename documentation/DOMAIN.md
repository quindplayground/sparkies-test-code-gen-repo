# Domain layer — smoke-health-api (skeleton)

Domain models, rules, and ports will live under `smoke_health_api/domain/` once implemented. See `documentation/STRUCTURE.md` for how this layer fits the architecture.
